services:
  backend:
    build: .
    environment:
      DJANGO_SECRET_KEY: ${DJANGO_SECRET_KEY:-django-insecure-local-development-only}
    ports:
      - "127.0.0.1:8000:8000"
    volumes:
      - .:/app
